\## 完成了485串口通信最基本最核心的功能。

1.软件刚打开的时候会自动发现串口。如果软件打开时，未识别到串口，可以点击刷新。

2.串口通信的\*\*参数\*\*可选。波特率、停止位、数据位、校验位、数据流。

3.支持ASCll码和Hex码的读写。

4.串口在20ms没有接收数据时，会认为这一条数据已经结束。以此逻辑作为判断一条数据的终点。



\## 版本规划

V0.2  读写合并显示。

&#x09;汉字 ASCll码解析会出错。汉字友好型。自动检测，友好提示。

V0.3  增加协议解析功能

V0.4 Modbus RTU协议

V0.5 重写界面

